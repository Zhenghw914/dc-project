export const setSampNumber = () => {};
// 设置频谱数据起始频段
export const setBgnAndEndFreq = (BgnFreq, EndFreq) => {
  window.setData.setBgnAndEndFreq(BgnFreq, EndFreq);
};

// Wifi定位
export const WiFiStart = (uiPosChannel, aucPosMac) => {
  window.setData.WiFiStart(uiPosChannel, aucPosMac);
};

// Wifi取消定位
export const WiFiStop = (uiPosChannel, aucPosMac) => {
  window.setData.WiFiStop(uiPosChannel, aucPosMac);
};

// 设备软复位
export const sendMsgSoftResetRsp = () => {
  window.setData.sendMsgSoftResetRsp();
};
