<template>
  <header class="header">
    <!-- 左侧 LOGO 和标题 -->
    <div class="header-left">
      <img src="@/assets/logo.png" alt="Logo" class="logo" />
      <span class="title">电磁移动平台</span>
    </div>

    <!-- 中间时间 -->
    <div class="header-center">
      {{ currentTime }}
    </div>

    <!-- 右侧日期 -->
    <div class="header-right">
      {{ currentDate }}
    </div>
  </header>
</template>

<script setup>
import { ref, onMounted } from 'vue';

const currentTime = ref('');
const currentDate = ref('');

// 更新时间
const updateTime = () => {
  const now = new Date();
  currentTime.value = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
  currentDate.value = now.toLocaleDateString('zh-CN', {
    weekday: 'long',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit'
  });
};
// 每分钟更新一次时间
onMounted(() => {
  updateTime();
  setInterval(updateTime, 1000 * 60);
});
</script>

<style scoped lang="scss">
.header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 60px;
  background-color: #2196f3;
  color: white;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  z-index: 1000;
}

/* 左侧LOGO和标题 */
.header-left {
  display: flex;
  align-items: center;
  flex-shrink: 0;  /* 避免LOGO区域收缩 */
  min-width: 200px;  /* 固定左侧最小宽度 */
}

.logo {
  height: 40px;
  margin-right: 10px;
}

.title {
  font-size: 20px;
  font-weight: bold;
  font-style: italic;
}

/* 中间时间，自动占用剩余空间 */
.header-center {
  flex: 1;  /* 占满剩余空间 */
  text-align: center;
  font-size: 18px;
}

/* 右侧日期固定宽度，防止被挤压 */
.header-right {
  flex-shrink: 0;
  white-space: nowrap;  /* 避免换行 */
  min-width: 200px;  /* 固定最小宽度 */
  font-size: 14px;
}
</style>
